import React from "react";

export function Instructions(): JSX.Element {
    return (
        <div>
            You can use the following commands:
            <pre>cd location</pre>
        </div>
    );
}
